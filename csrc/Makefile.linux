include common.mk


libmapnik-jni.so: mapnikjni.cpp mapnikjni.h
	$(CXX) -o libmapnik-jni.so \
		-fPIC -shared \
		$(CFLAGS) \
		$(MAPNIK_CFLAGS) \
		$(JAVA_CFLAGS) \
		mapnikjni.cpp \
		$(LDFLAGS) \
		$(MAPNIK_LIBS)
	
clean:
	rm libmapnik-jni.so
	
